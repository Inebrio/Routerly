## Session: 2026-06-24 (continued — release/0.3.0 feature implementation + bug fixes)

### Done
- Created release/0.3.0 branch from develop
- Implemented and merged 11 feature groups into release/0.3.0:
  - #85 — OpenAI Web + Anthropic Web session providers (feat/issue-85-web-session-providers)
  - #94/#95/#96 — Usage enrichment: endUserId, sessionId, custom tags (feat/issue-96-95-94-usage-enrichment)
  - #97 — Provider-native prompt caching: auto/passthrough/disabled (feat/issue-97-provider-prompt-caching)
  - #93 — Prometheus metrics endpoint /metrics (feat/issue-93-prometheus-metrics)
  - #101 — Provider health dashboard page (feat/issue-101-provider-health-dashboard)
  - #78 — Per-agent routing policies via X-Routerly-Policy header (feat/issue-78-per-agent-routing-policies)
  - #82 — Hierarchical spend limits / spend groups (feat/issue-82-hierarchical-spend-limits-wt)
  - #80 — Model leaderboard dashboard page + /api/leaderboard (feat/issue-80-model-leaderboard)
  - #77/#76 — Content guardrails + PII scrubbing middleware (feat/issue-77-76-guardrails-pii)
  - #89/#90/#91 — Notification system: event taxonomy, routing rules, cooldown, in-app inbox (feat/issue-89-90-91-notifications)
- Additional features completed in retrospective coverage pass (feat/release-03-cli-coverage):
  - #84 — 2FA TOTP enrollment (service + dashboard + CLI)
  - #102 — Prompt management with versioning (service + dashboard + CLI)
  - #92 — Permissions hardening + audit log (service + dashboard + CLI)
  - #87 — Enterprise cloud providers: Bedrock, Azure OpenAI, Vertex (service)
  - #88 — Notification channel adapters: Slack, Teams, PagerDuty, Discord (service)
  - #81 — Model discovery page (dashboard + static catalog)
  - #75 — Semantic response caching (service)
  - #99 — Prompt playground with compare mode and presets (dashboard + CLI)
- All feature branches pushed to origin and linked to GitHub issues
- Created RELEASE-0.3.0.md
- Dashboard/2FA/notification bug fixes (commit 838eb1c):
  - Notification dropdown: fixed sidebar overflow:hidden clipping via React portal + z-index 9999
  - 2FA setup: replaced broken Google Charts QR image with step-by-step instructions + mobile link
  - 2FA: ProfilePage now syncs totpEnabled state to AuthContext on confirm/disable
  - Settings > General: added "Require 2FA for all users" toggle (admin-configurable)
  - App: force-enrollment banner when requireMfa=true and user has no 2FA
  - CLI auth login: added TOTP challenge step when server returns requiresTotp:true
  - Service login/2fa-verify responses: now include totpEnabled in user object
  - shared Settings type: added requireMfa field

### Discovered
- API overload (529) when running >2 parallel agents — cap at 2 concurrent
- Commit subjects must be fully lowercase after the colon (commitlint)
- Worktrees from failed agents must be cleaned with `git worktree remove --force` before retrying
- shared/types/config.ts is large — agents must read it before adding fields to avoid duplication
- Dashboard is served from built dist/ files — source changes require `npm run build --workspace=packages/dashboard`
- Google Charts QR API is deprecated/dead — never use it; use otpauth:// link or server-side generation

### Remaining (ordered by complexity)
1. #103 — Request pipeline architecture (pre/post processing hooks — internal refactor, no UI)
2. #83 — External auth: OAuth2/OIDC/LDAP/SAML (complex, needs provider config UI)
3. #86 — Horizontal scaling: shared state abstraction with Redis adapter
4. #79 — MCP Gateway: route and observe Model Context Protocol traffic

### Open problems
- Tests: 2 pre-existing failures in model.test.ts (model discover command, unrelated to recent changes)
- The remaining 4 features (#103, #83, #86, #79) are architecturally complex; each should be a full feature branch
