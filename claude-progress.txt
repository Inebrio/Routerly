## Session: 2026-06-25 (piano sequenziale per issue — conferma prima di procedere)

### Completati (release/0.3.0)
- #85 — OpenAI Web + Anthropic Web session providers
- #94/#95/#96 — Usage enrichment: endUserId, sessionId, custom tags
- #97 — Provider-native prompt caching
- #93 — Prometheus metrics /metrics
- #101 — Provider health dashboard
- #78 — Per-agent routing policies via X-Routerly-Policy
- #80 — Model leaderboard page + /api/leaderboard
- #89/#90/#91 — Notification system
- #84 — 2FA TOTP enrollment (service + dashboard + CLI)
- #102 — Prompt management with versioning — REVERTED (commit b0a8f66), not on release/0.3.0
- #87 — Enterprise cloud providers: Bedrock, Azure OpenAI, Vertex
- #88 — Notification channel adapters: Slack, Teams, PagerDuty, Discord
- #81 — Model discovery page
- #75 — Semantic response caching
- #99 — Prompt playground
- Dashboard bug fixes (commit 838eb1c): notification z-index, 2FA QR, requireMfa toggle

---

## Piano feature (sequenziale — conferma dopo ogni feature)

---

### CORRENTE: #92 — Permissions hardening + audit log
URL: https://github.com/Inebrio/Routerly/issues/92
Stato GitHub: OPEN
Stato agente: VERIFIED PARTIAL

**Cosa chiedeva la issue:**
1. Aggiungere permessi mancanti (settings:read/write, notification:write, token:read/write, role:write, audit:read)
2. Dashboard UI per custom roles con i nuovi permessi
3. Enforcement project-level roles lato service
4. Audit log: recording, API /api/audit, dashboard page con filtri
5. Credential hygiene: token expiry (expiresAt), last-used timestamp

**Risolto in questa sessione:**
- packages/dashboard/src/api.ts: ALL_PERMISSIONS esteso a 14 permessi
- packages/dashboard/src/pages/RolesPage.tsx: PERM_LABELS aggiornato (14 label)
- packages/dashboard/src/api.ts: ProjectToken interface aggiunta expiresAt + lastUsedAt
- packages/dashboard/src/pages/project/ProjectTokenTab.tsx: colonne "Last Used" + "Expires"
- packages/service/src/plugins/auth.ts: expiry check + lastUsedAt tracking su ogni auth
- packages/service/src/routes/api.ts: POST tokens ora accetta expiresAt (Zod, futuro only)
- 6 nuovi test aggiunti (auth.test.ts + api.test.ts), 1306 test totali, tutti green

**Verificato dall'agente (browser + service):**
- /api/roles: Admin ha 14 permessi ✓
- /api/audit: HTTP 200 ✓
- Token con expiresAt passato: HTTP 400 ✓
- Settings > Roles: tutti e 14 i permessi visibili nei badge ✓
- New Role form: 14 checkbox correttamente labeled ✓
- Audit Log page: carica, empty state "No audit entries found." ✓
- Token tab: colonne Created / Last Used / Expires visibili ✓
- Nessun errore console ✓

**Bug aggiuntivi trovati e fixati durante UAT:**
- Service: GET/PUT /api/settings usava `user:write` invece di `settings:read`/`settings:write` → fixato
- Dashboard: SettingsGeneralTab crashava con null settings → fixato (early return su errore)
- Dashboard: OverviewPage spinner infinito per utenti senza report:read → fixato ("No permission to view usage data.")

**UAT utente auditor (solo settings:read + audit:read) — VERIFIED:**
- Login: OK ✓
- Overview: "No permission to view usage data." (graceful, non crash) ✓
- Audit Log: carica, filtri funzionanti ✓
- Settings General: carica correttamente (settings:read ora funziona) ✓
- /api/audit: HTTP 200 ✓
- /api/roles POST: HTTP 403 ✓
- /api/projects POST: HTTP 403 ✓
- /api/users: HTTP 403 ✓
- Ruolo "Auditor" creato dalla UI con solo 2 permessi → compare in lista ✓

**Stato finale: VERIFIED DONE** (salvo conferma utente)

---

### #77 — Content Guardrails (full redesign + refinements round 2)
URL: https://github.com/Inebrio/Routerly/issues/77
Stato: VERIFIED DONE (2026-06-26) — service+browser+CLI tutti eseguiti; 1 bug streaming fixato. Attende sign-off utente. Non committato.

**Redesign completo + raffinamenti post-review (round 2):**
1. "Rules" rinominate "Security Policies" in tutta la UI
2. Rimosso toggle `enabled` per-policy (presenza = attivo); `enabled` reso opzionale nel Zod schema per backward compat
3. "Add a security policy..." ora usa SearchableSelect + dashed border (come Routing tab), senza badge colorati
4. Moderation: JSON format instruction sempre appesa in fondo al system prompt, anche se custom
5. Tutte le policy girano in parallelo (`Promise.all`, non più `for...of`)
6. Model select mostrano TUTTI i modelli configurati (non solo quelli del progetto)
7. Semantic: solo modelli embedding (`capabilities.embedding === true`)
8. CLI: rimosso `--toggle-rule`; tabella rules non mostra più colonna Enabled
9. Docs aggiornati: terminologia, parallel execution, moderation format, model selection

**File modificati:**
- `packages/shared/src/types/config.ts`: GuardrailRule.enabled → opzionale
- `packages/dashboard/src/api.ts`: GuardrailRule.enabled → opzionale
- `packages/service/src/routes/api.ts`: enabled: z.boolean().optional() su tutti e 4 i tipi
- `packages/service/src/middleware/guardrails.ts`: Promise.all, moderation JSON always appended, rimosso r.enabled && filter
- `packages/service/src/middleware/guardrails.test.ts`: rimosso enabled: true da factory
- `packages/dashboard/src/pages/project/ProjectSecurityTab.tsx`: terminologia, no enabled, no colori, SearchableSelect picker, getModels(), embeddingModelOptions
- `packages/cli/src/commands/project.ts`: rimosso --toggle-rule, rimossa colonna Enabled
- `docs/dashboard/projects.md` + `docs/cli/commands.md`: aggiornati

**Verificato (browser + service + tests):**
- Service tests: 1312/1312 passano ✓
- Typecheck: shared, service, cli, dashboard tutti clean ✓
- Dashboard Security tab: "SECURITY POLICIES" header, no enabled checkbox, badge plain (no color), SearchableSelect dashed-border picker ✓
- Semantic: dropdown mostra solo modelli embedding (text-embedding-3-small/large/ada-002) ✓
- Topic/Moderation: dropdown mostra tutti i modelli ✓
- Save: HTTP 200, senza campo enabled nella request ✓
- Service: regex "competitor" → HTTP 400 guardrail_blocked ✓
- Service: injection "ignore previous instructions" → HTTP 400 guardrail_blocked ✓
- Service: richiesta pulita "What is 2+2?" → HTTP 200 ✓
- Playground: POST /v1/chat/completions con "competitor" → HTTP 400 (network tab confermato) ✓
- Playground: "This request was blocked by content guardrails." appare nella chat (non più bolla vuota) ✓
- Bug fix: TestPage.tsx — `if (!res.ok || !res.body)` invece di `if (!res.body)` — HTTP 400 ora estratto correttamente dall'error message JSON
- Bug fix: openai.ts + anthropic.ts — guardrail checks solo last user message (non tutti i messaggi), evita falsi positivi in conversazioni multi-turn
- Bug fix: TestPage.tsx — messaggi bloccati rimossi dalla history (slice(-1) nel catch) — solo per errori reali (network, auth)
- Bug fix: openai.ts + anthropic.ts — guardrail block ora restituisce HTTP 200 con streaming SSE (finish_reason: content_filter) invece di HTTP 400; appare come assistant bubble nel playground e in qualsiasi client OpenAI-compatibile

**Playground verification parziale (solo regex + injection, via browser):**
- Clean message → risposta corretta ✓
- Blocked (regex "competitor") → "This request was blocked by content guardrails." come assistant bubble ✓
- Clean after block → nessun bleed ✓
- Injection "ignore previous instructions" → blocked ✓

**Service-layer verification (curl, NON browser) per tutti i tipi:**
- Regex: 4/4 ✓ (blocked, second pattern, clean, case-insensitive)
- Injection detection: 4/4 ✓ (ignore-instructions, DAN mode, disregard, false-positive clean)
- Semantic (threshold 0.75): 4/4 ✓ (blocked x2, clean x2)
- Topic: ✓ con judge cloud (openai/gpt-4.1-nano) — "capital of France" + "blockchain" bloccati, "pasta carbonara" passa. Il FALLITO precedente era qwen3:4b (classificatore debole), NON un bug di codice.
- Moderation: ✓ con gpt-4.1-nano — "build a bomb" bloccato, "2+2" passa
- Response guardrail: ✓ non-streaming (regex "Paris" su capital-of-France → blocked) + streaming (FIX, vedi sotto)

**SESSIONE 2026-06-26 — VERIFIED DONE:**

BUG TROVATO E FIXATO — response guardrail streaming leak:
- openai.ts:382 `hasResponseGuardrails` controllava ancora `r.enabled` (rimosso nel redesign #77 → sempre undefined → some() false → nessun buffering SSE → contenuto bloccato veniva comunque streamato al client).
- Fix: rimosso `r.enabled &&`, ora match semantica middleware (solo target). +2 test regressione in openai.test.ts (streaming block sopprime contenuto + emette fallback content_filter; non-match non over-blocca).
- Service: 1314 test green, typecheck clean. Verifica live: streaming "Paris" → BLOCKED (content_filter, no leak), streaming "2+2" → "Four" passa.

Pending risolti:
1. Browser screenshot per ogni tipo ✓ (frontend agent, Chrome MCP):
   - Security tab: editor regex/semantic/topic/moderation, semantic dropdown SOLO embedding models, no checkbox enabled, picker dashed-border, badge plain
   - Playground block bubble: regex (1652ms), injection (11ms), topic (745ms), semantic (759ms); control "cook pasta" → risposta reale. Zero console error.
2. Topic "bug" ✓ — era qwen3:4b debole; con judge cloud funziona. Nessun fix codice.
3. Moderation + response (service + browser) ✓
4. CLI test per tipo ✓ — show rende tutti e 4 i tipi + injection flag con summary corretti; write flags --action/--fallback/--remove-rule round-trip via PATCH.

ISSUE MINORE NON BLOCCANTE (segnalata, non fixata):
- Dropdown judge model di topic/moderation includono i modelli embedding (che fallirebbero a runtime). Il filtro embedding-only è applicato solo al picker semantic. Cosmetico; selezionare un embedding come judge fallisce gracefully (rule skipped). Decidere se filtrare.

Cleanup post-verifica: test project guardrails+pii riportati a null (stato originale). CLI store: ripulito, ri-login come alias "default" (le sessioni info/info-2 erano scadute; backup in scratchpad).

NOTA HARDWARE: Ollama box 192.168.1.137 DOWN durante la sessione → judge locali (qwen3:4b) inutilizzabili. Tutta la verifica topic/moderation usa modelli cloud. NON un problema del codice guardrail.

Stato: #77 VERIFIED DONE (in attesa sign-off utente sulle evidenze). Modifiche NON committate.

**PENDING #76/#77 — richieste utente aperte (NON ancora implementate):**

1. WIRE FORMAT del blocco deve combaciare ESATTAMENTE con lo standard provider (decisione utente: "tutto esattamente come da standard, come si comportano OpenAI/Anthropic"):
   - Anthropic /v1/messages: oggi stop_reason="end_turn" + text frase. Standard = HTTP 200, stop_reason="refusal", content:[] vuoto, opzionale stop_details{type,category,explanation}. (fonte: claude-api skill, autorevole)
   - OpenAI /v1/chat/completions: oggi finish_reason="content_filter" (OK) ma content=frase. Standard = content null/vuoto.
   - fallbackMessage NON deve uscire sul wire: spostarlo lato dashboard. Playground (TestPage.tsx) deve rilevare finish_reason=content_filter / stop_reason=refusal e renderizzare lui la bolla "blocked".
   - File: packages/service/src/routes/openai.ts + anthropic.ts (tutti i path block: request, response non-stream, response stream) + packages/dashboard/src/pages/TestPage.tsx
   - Cross-check con CLAUDE.md "Provider response wire format to client is never altered".

2. TRACE DEBUG completo per guardrail E PII (richiesta utente):
   - Guardrail: GIA' nel trace (request guardrail:triggered, response guardrail:response-triggered, stream+non-stream, openai+anthropic) ✓
   - PII input scrub: GIA' nel trace (pii:scrubbed con entities) ✓
   - PII OUTPUT scrub: MANCA nel trace (solo request.log.info). Aggiungere appendTrace panel:'response' message:'pii:scrubbed' details:{entities:found} in 3 punti:
     * openai.ts:601-609 (non-stream)
     * openai.ts:376-410 (streaming StreamingScrubber — accumulare found tra i chunk)
     * anthropic.ts:138-147 (non-stream)

Verifica richiesta dopo impl: curl (status+body block per ogni superficie) + browser (Playground bolla blocked + pannello Debug mostra guardrail e PII input+output) + test + typecheck. In attesa conferma utente per procedere con la delega (service + frontend agent).

**STATO FIX (2026-06-26, sessione fix) — VERIFICATO orchestratore:**
- Wire format standard: FATTO+VERIFICATO (OpenAI 200 content_filter content vuoto; Anthropic 200 stop_reason refusal content[] stop_details; x-routerly-trace-id header su blocchi). curl ok.
- BUG-1: FATTO+VERIFICATO browser. Box rosso "GUARDRAIL BLOCKED" con rule/target/action/fallbackMessage nella bolla E nei Technical Details (non piu' vuoti). PII nei Technical Details ("PII SCRUBBED" + badge entita'). TraceEntryRenderer.tsx + TestPage.tsx + getTrace.
- BUG-3: FATTO (regex phone internazionale, test agent).
- BUG-4: FATTO+VERIFICATO. Judge/embedding contati sul progetto reale, gated da checkBudget.
- BUG-5: FATTO+VERIFICATO browser. callType 'guardrail'; /api/usage summary guardrailCalls/guardrailCost; card GUARDRAIL CALLS nella pagina Usage; pruneOrphanUsage all'avvio (18 orfani rimossi). UsagePage.tsx.
- Test: service 1336 verdi, dashboard 26 verdi, typecheck pulito (shared/service/cli/dashboard).
- PRE-ESISTENTI non correlati: 2 fail in packages/cli/src/commands/model.test.ts (model discover free/local label + 404 fallback) — model.ts NON toccato, fuori scope #76/#77.

NIT RIFINITURA RIMASTI (minori):
- N1: header box rosso "RESPONSE BLOCKED BY GUARDRAIL" anche su blocco request -> rendere context-aware (REQUEST vs RESPONSE da target).
- N2: filtro TYPE pagina Usage manca opzione "Guardrail" (card c'e', filtro no).
- N3: Debug panel resta stale dopo Clear (Clear pulisce chat ma non il Debug dei turn precedenti).
- COVERAGE: dashboard ha solo 26 test; nuova logica (TraceEntryRenderer block/pii, TestPage block detection, UsagePage card) probabilmente senza test -> verificare coverage >=98%.
- DOCS: aggiornare docs/api/llm-proxy.md (nuovo wire format blocco), docs guardrails (judge contati/limitati + callType guardrail + /api/usage fields).

---

**BUG TROVATI DURANTE DEMO BROWSER (2026-06-26) — da risolvere dopo:**

BUG-1 [trace blocco invisibile nel Playground]: quando una richiesta viene BLOCCATA da guardrail (regex/injection/semantic/topic/moderation), il turn nel pannello Debug del Playground ha "Technical Details" VUOTO — nessuna entry di trace mostrata. Il `guardrail:triggered` viene chiamato lato server (appendTrace openai.ts:149 / anthropic.ts:34) ma NON arriva alla UI per la risposta bloccata.
  - Causa probabile: il path di blocco restituisce una SSE sintetica (fallback) che ritorna early SENZA setTrace finale / senza header x-routerly-trace-id, quindi il Playground non riesce a fetchare il trace per quel turn. I turn NON bloccati mostrano il trace pieno (router:intake/result, model:request/success).
  - Impatto: viola la richiesta utente "il processo di guardrail e PII deve essere nel trace di debug". Anche se appendTrace c'e', la UI non lo vede sui blocchi.
  - File: packages/service/src/routes/openai.ts + anthropic.ts (block path: assicurare setTrace + header trace-id sulla risposta di blocco), eventualmente packages/dashboard/src/pages/TestPage.tsx (fetch trace anche su risposta bloccata).
  - Osservato su: regex block (TURN #1) e injection block (TURN #3) — entrambi Technical Details vuoto.
  - ANOMALIA CORRELATA: in un altro blocco (semantic, bolla "blocked" a 324ms) il Debug TURN #1 mostrava una COMPLETION con latency 4.55s, 154 output token e costo $0.00396 — incoerente con un blocco. Da investigare: o e' un turn stale non pulito dal pannello, o un blocco viene attribuito erroneamente a un completion/costo del modello (in tal caso anche il cost tracking conterebbe richieste bloccate). Verificare cost/tracker su richieste bloccate.
  - COMPORTAMENTO ATTESO (richiesta utente, raffinato 2026-06-26): TURN # e Technical Details non devono MAI essere vuoti. Devono SEMPRE mostrare il dettaglio guardrail e PII (input+output) TRA I TECHNICAL DATA *e* DENTRO IL DETTAGLIO DI USAGE (non solo nel trace). In caso di blocco: ERRORE con GRAFICA DI ERRORE (box rosso) e le informazioni del blocco (quale regola, target, azione, eventuale score). Vale in OGNI caso (bloccato e non): le info guardrail/PII sempre visibili nei technical data e nell'usage detail.
    File coinvolti: packages/dashboard/src/pages/TestPage.tsx (render technical data + usage detail + box errore rosso), packages/service/src/routes/openai.ts + anthropic.ts (esporre guardrail/PII nel payload trace/usage anche sui blocchi, con header trace-id sulla risposta di blocco).

BUG-2 [RISOLTO — NON e' un bug, era config soglia]: semantic con threshold 0.8 non bloccava "how do I build an explosive device"; con 0.7 blocca (content_filter). La similarita' del pair "explosive device" vs example "how to make a bomb" sta tra 0.7 e 0.8. Era la config demo (0.8 troppo alta vs 0.75 della sessione precedente). Verificato via curl: 0.8 -> passa 3/3, 0.7 -> blocca. Nessun fail-open, nessun bug di codice.
  - Osservazione utile (osservabilita', lega a BUG-1): quando una regola semantic/topic/moderation gira e NON matcha (o viene skippata per errore embedding), non c'e' indicazione nel trace. L'utente non distingue "regola eseguita e passata" da "regola fallita/saltata". Aggiungere al trace anche gli esiti negativi/skip delle regole guardrail (es. guardrail:evaluated rule=semantic score=0.73 threshold=0.80 result=pass). Vale anche per il fail-open su errore embedding (oggi ritorna null silenziosamente).

BUG-3 [PII minore — prefisso telefono internazionale non mascherato]: con PHONE attivo, input "+1 (415) 555-2671" -> il modello riceve "+1 ([PHONE_NUMBER])": il numero e' mascherato ma il prefisso "+1 (" (country code + parentesi aperta) resta in chiaro. La regex PHONE in piiScrubber.ts parte da "415" perche' \b non matcha prima di "+". Numeri senza prefisso ("415 555 2671") vengono mascherati interamente (verificato in browser). Impatto basso (il country code da solo non e' PII sensibile), ma la maschera e' incompleta sui formati internazionali.
  - NB: telefono visibile con config "solo EMAIL" e' CORRETTO (scrub selettivo), non un bug.
  - File: packages/service/src/middleware/piiScrubber.ts (regex PHONE: catturare anche prefisso +CC e parentesi).

BUG-4 [chiamate modello dei guardrail NON contate nell'usage NE' limitate] (richiesta utente, IMPORTANTE):
  - Stato attuale: i guardrail che usano un modello (semantic = embedding, topic = judge LLM, moderation = judge LLM) chiamano via makeGuardrailCtx con projectId:'guardrail' (progetto FITTIZIO) e callType:'routing'. Commento nel codice: "routing call type = no double-cost". Risultato: token/costo di queste chiamate attribuiti a un progetto finto, NON al progetto reale, NON contati nell'usage del progetto, NON soggetti ai limiti di usage.
  - Comportamento atteso: ogni chiamata a modelli fatta dai guardrail (semantic intent/embedding, moderation, topic, e qualsiasi judge) DEVE: (a) essere conteggiata nell'utilizzo token del progetto reale, (b) essere soggetta ai limiti di usage del progetto e BLOCCATA se li supera (come una normale completion).
  - File: packages/service/src/middleware/guardrails.ts (makeGuardrailCtx: usare projectId reale + token reale + callType appropriato invece del progetto fittizio), packages/service/src/llm/executor.ts + packages/service/src/cost/tracker.ts (assicurare recording usage e check limiti anche per queste chiamate), verificare il punto in cui i limiti vengono valutati (pre-call) perche' il guardrail gira PRIMA della completion principale.
  - Nota progettazione: valutare se contare l'embedding del semantic (che oggi passa per classifyIntent/getEmbeddingProvider, non per llmChat) — anche quello e' una chiamata a un modello a pagamento.
  - Impatto: COSTI/LIMITI — un progetto puo' superare i limiti via guardrail senza che vengano conteggiati; costi non tracciati.
  - STATO: codice FIXATO (makeGuardrailCtx ora usa progetto reale; judge contati + gated via checkBudget; embedding semantic contato via trackUsage). Verificare con curl che un blocco topic/moderation produca un record usage sul progetto REALE.

BUG-5 [progetto "guardrail" fantasma nell'usage] (richiesta utente):
  - In usage compare un progetto "guardrail" che NON esiste in projects.json. E' il residuo del vecchio projectId:'guardrail' (pre-fix BUG-4). usage.json (~/.routerly/data/usage.json) contiene 18 record storici con projectId:"guardrail" (tutti vecchi, qwen3:4b, outcome error, cost 0). Il codice nuovo non ne crea piu'.
  - Concetto utente: il guardrail e' una SOTTO-ATTIVITA' di un progetto, non un progetto. Quindi: (a) i record dei guardrail vanno attribuiti al progetto reale (FATTO da BUG-4), (b) marcare le chiamate guardrail con un callType/sottotipo distinto (es. callType 'guardrail' o un campo subActivity) cosi nell'usage detail si vede il contributo guardrail/PII separato dalla routing/completion normale (lega a BUG-1: "info guard rail e pii ... dentro il dettaglio di usage"), (c) PULIZIA dei 18 record orfani 'guardrail' da usage.json.
  - File: packages/service/src/middleware/guardrails.ts (callType dedicato), packages/service/src/cost/tracker.ts (supporto sottotipo nell'aggregazione usage), packages/dashboard (usage detail mostra il sottotipo), + migrazione/pulizia dati usage.json.

---

### PROSSIMA: #82 — Spend Groups dashboard page

### #82 — Spend Groups dashboard page
URL: https://github.com/Inebrio/Routerly/issues/82
Stato: CRUD API /api/spend-groups implementata, nessuna UI nel dashboard

**Cosa chiedeva:** gestione gerarchica dei limiti di spesa (org → team → project)
**Cosa manca:** SpendGroupsPage + collegamento in sidebar/settings + api.ts functions
**Test:** Settings > Spend Groups > crea gruppo con limite $100/mese

---

### #98 — A/B testing (traffic splitting)
URL: https://github.com/Inebrio/Routerly/issues/98
Stato: non implementato

**Cosa chiedeva:** split del traffico tra modelli con percentuali configurabili
**Complessità:** MEDIA — nuova routing policy + UI per progetto

---

### #103 — Request pipeline architecture
URL: https://github.com/Inebrio/Routerly/issues/103
Complessità: ALTA

### #83 — External auth (OAuth2/OIDC/LDAP/SAML)
URL: https://github.com/Inebrio/Routerly/issues/83
Complessità: ALTA

### #86 — Horizontal scaling (Redis adapter)
URL: https://github.com/Inebrio/Routerly/issues/86
Complessità: ALTA

---

### Scoperto durante le sessioni
- API overload (529) con >2 agenti paralleli — limite a 2 concorrenti
- Commit subjects lowercase dopo i due punti (commitlint)
- shared/types/config.ts grande — leggerlo prima di aggiungere campi
- Dashboard servita da dist/ — `npm run build --workspace=packages/dashboard` dopo ogni modifica
- Google Charts QR API morta — non usarla mai

### Problemi aperti
- Tests: 2 failures pre-esistenti in model.test.ts (model discover)
- #103 / #83 / #86 sono architetturalmente complessi — branch separati

---

## ATTIVITA' MANCANTI (post fix #76/#77, 2026-06-26)
1. COVERAGE dashboard <98%: nuovo codice guardrail/PII testato (TraceEntryRenderer/UsagePage/TestPage = 19 test), ma le pagine pre-esistenti (SSE streaming, compare mode, preset, timeline) restano scoperte. Globale era 7.7% prima. Giro di test separato.
2. CLI 2 failures PRE-ESISTENTI: model.test.ts (model discover "free/local" + 404). model.ts non toccato. Fix a parte.
3. WIRE FORMAT BREAKING: blocco guardrail ora ritorna content VUOTO (OpenAI content_filter / Anthropic refusal) invece del testo fallback. Rottura per client che leggevano il testo nel content. Documentare in release notes; fallbackMessage ora solo nel trace.
4. CHANGESET mancante per #76/#77 + fix (CLAUDE.md usa Changesets).
5. SEMANTIC EMBEDDING non pre-gated: token contati (trackUsage) ma NON passano per checkBudget. Topic/moderation gated. Decidere se gateare anche embedding.
6. DOC SCREENSHOT: verificare che screenshot-usage.png mostri il filtro "Guardrail" (N2 aggiunto dopo); rigenerare se predatato.
7. callType 'guardrail' nel breakdown report CLI (oggi solo dashboard + /api/usage).
