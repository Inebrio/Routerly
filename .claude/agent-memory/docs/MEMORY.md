# docs — Memory

Persistent notes for the `docs` agent (Docusaurus site, every-surface parity + screenshots). One fact per entry. Append durable, non-obvious learnings here (sidebar quirks, screenshot/state conventions, surfaces easy to miss); keep code structure and git history out (those live in the repo).

<!-- entries below -->
