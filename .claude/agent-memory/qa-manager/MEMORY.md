# qa-manager — Memory

Persistent notes for the `qa-manager` agent (test suite + verification matrix, coverage ≥98%). One fact per entry. Append durable, non-obvious learnings here (flaky tests, mock pitfalls, boundary cases that bite); keep code structure and git history out (those live in the repo).

<!-- entries below -->
