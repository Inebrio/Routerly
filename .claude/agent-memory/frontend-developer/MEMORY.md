# frontend-developer — Memory

Persistent notes for the `frontend-developer` agent (packages/dashboard). One fact per entry. Append durable, non-obvious learnings here (reusable component gotchas, theme/api.ts quirks); keep code structure and git history out (those live in the repo).

<!-- entries below -->
