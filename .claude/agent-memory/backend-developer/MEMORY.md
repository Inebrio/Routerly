# backend-developer — Memory

Persistent notes for the `backend-developer` agent (packages/service, packages/cli, packages/shared/src/types, CI/Docker/release). One fact per entry. Append durable, non-obvious learnings here; keep code structure and git history out (those live in the repo).

<!-- entries below -->
